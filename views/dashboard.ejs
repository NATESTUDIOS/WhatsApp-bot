<!DOCTYPE html>
<html>
<head>
  <title>WhatsApp Tagging Bot</title>
  <style>
    body { font-family: sans-serif; text-align: center; margin-top: 50px; }
    img { margin-top: 20px; border-radius: 8px; }
    .status { font-size: 1.2em; margin-top: 20px; }
  </style>
</head>
<body>
  <h1>🤖 WhatsApp Tagging Bot</h1>
  <% if (qrCode) { %>
    <p>Scan this QR with your WhatsApp:</p>
    <img src="<%= qrCode %>" alt="QR Code" width="300">
  <% } else if (isReady) { %>
    <p class="status">✅ Bot is connected and ready!</p>
    <form action="/tagall" method="get">
      <button type="submit">Tag Everyone</button>
    </form>
  <% } else { %>
    <p class="status">Connecting...</p>
  <% } %>
</body>
</html>
